table! {
    hotel (id) {
        id -> Int4,
        name -> Varchar,
        description -> Varchar,
        category -> Varchar,
        address -> Varchar,
        city -> Varchar,
        state -> Varchar,
        zip -> Varchar,
        phone -> Varchar,
        website -> Varchar,
        email -> Varchar,
        price -> Int4,
        rating -> Varchar,
        review_count -> Varchar,
        latitude -> Varchar,
        longitude -> Varchar,
        rooms -> Int4,
        image -> Varchar,
    }
}
